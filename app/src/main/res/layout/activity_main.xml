<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.hs.picassodiskcache.MainActivity">

    <Button
        android:id="@+id/cache_image_btn"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:onClick="cacheImages"
        android:text="@string/cache_images" />

    <Button
        android:id="@+id/show_image_btn"
        android:layout_below="@id/cache_image_btn"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="@string/show_images"
        android:onClick="showImages"
        android:layout_centerHorizontal="true"/>

    <Button
        android:id="@+id/clear_cache_btn"
        android:layout_below="@id/show_image_btn"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="@string/clear_cache"
        android:onClick="clearCache"
        android:layout_centerHorizontal="true"/>

    <ListView
        android:layout_below="@id/clear_cache_btn"
        android:id="@+id/images_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</RelativeLayout>
